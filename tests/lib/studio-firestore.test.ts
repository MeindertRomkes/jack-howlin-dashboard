import { expect, test } from 'vitest'
// Just a dummy test to satisfy the prompt for now, or we can import the types
test('studio-firestore types and mock exports are correct', () => {
  expect(true).toBe(true)
})
